# Dependências do backend (fonte única).
#
# A Vercel lê este arquivo da raiz para montar a Vercel Function que roda
# back-end/app.py (ver api/index.py). Localmente o mesmo install é feito por
# `pip install -r back-end/requirements.txt`, que aponta para cá.
#
# Versões fixadas para o deploy não sair do controle: é o que está instalado e
# testado no venv local.

Flask==3.1.3
flask-cors==6.0.5
requests==2.34.2
matplotlib==3.11.1
yfinance==1.7.0
python-dotenv==1.2.2
